name: builder
description: Development utility for owvision, which can build and compile the whole workspace.
version: 0.0.13
publish_to: "none"

environment:
    sdk: ^3.9.2

# Add regular dependencies here.
dev_dependencies:
    yaml: ^3.1.3
    yaml_edit: ^2.2.3
    args: ^2.7.0
    shared: any
    chalk: ^1.2.1
    chalkdart: ^3.0.5
    daemon: any
    path: ^1.9.1

resolution: workspace
